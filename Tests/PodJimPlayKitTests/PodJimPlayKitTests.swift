import XCTest
@testable import PodJimPlayKit

final class PodJimPlayKitTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(PodJimPlayKit().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
